import { Meta, Canvas, Title, ArgTypes, Controls, Stories } from '@storybook/addon-docs/blocks';
import * as AlertStories from './Alert.stories';
import rawCode from '../../../Components/Alert.fs?raw';

<Meta of={AlertStories} />
<Title/>

```fsharp
Alert() {
    AlertTitle() {
        "Title"
    }
    AlertDescription() {
        "Alert description"
    }
}
```

<Canvas withToolbar={true} sourceState={"none"} />

### Alert props

<Controls />

<SourceCode code={rawCode} />

<Stories includePrimary={false}/>
