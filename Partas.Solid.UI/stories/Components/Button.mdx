import { Canvas, Meta, Controls, ArgTypes, Stories } from '@storybook/addon-docs/blocks';
import * as ButtonStories from './Button.stories';

<Meta of={ButtonStories} />

# Button

'shadcn-ui' styled button.

'Variant' and 'Size' are accessible via the 'Button' module.

```fsharp
Button.Variants.Default
Button.Size.Default
```

```fsharp
Button() {
    "Button"
}
```

<Canvas of={ButtonStories.Default} sourceState={"none"} layout={"centered"} withToolbar={true}/>

<Controls />

<Stories includePrimary={false} of={ButtonStories} />


