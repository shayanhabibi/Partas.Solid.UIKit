import { Meta, Canvas, ArgTypes, Controls, Title } from '@storybook/addon-docs/blocks';
import * as DrawerStories from './Drawer.stories';
import * as DrawerTriggerStories from './DrawerTrigger.stories';
import * as DrawerPortalStories from './DrawerPortal.stories';
import * as DrawerCloseStories from './DrawerClose.stories';
import * as DrawerOverlayStories from './DrawerOverlay.stories';
import * as DrawerContentStories from './DrawerContent.stories';
import * as DrawerHeaderStories from './DrawerHeader.stories';
import * as DrawerFooterStories from './DrawerFooter.stories';
import * as DrawerTitleStories from './DrawerTitle.stories';
import * as DrawerDescriptionStories from './DrawerDescription.stories';

<Meta of={DrawerStories} />
<Title/>

## Minimal Example


```fsharp
Drawer().spread props {
    DrawerTrigger() {
        Button() {
            "Click me"
        }
    }
    DrawerContent() {
        DrawerHeader() {
            DrawerTitle() {
                "Are you absolutely sure?"
            }
            DrawerDescription() { "This action cannot be undone." }
        }
        DrawerFooter() {
            Button() { "Submit" }
            DrawerClose() {
                Button(variant = Button.Variant.Outline, class'="w-full") {
                    "Cancel"
                }
            }
        }
    }
}
```

> Note, there appears to be some odd behaviour when using the 'side' prop, but
> I don't remember if this is the case in environments outside of storybook.
> I would normally reach for the sheet component when rendering from the side anyway.

<Canvas of={DrawerStories.Default} layout="centered" withToolbar={true} sourceState={"none"}/>
<Controls/>

## Components

### Drawer
<ArgTypes of={DrawerStories} />

### DrawerTrigger
<ArgTypes of={DrawerTriggerStories} />

### DrawerPortal
<ArgTypes of={DrawerPortalStories} />

### DrawerClose
<ArgTypes of={DrawerCloseStories} />

### DrawerOverlay
<ArgTypes of={DrawerOverlayStories} />

### DrawerContent
<ArgTypes of={DrawerContentStories} />

### DrawerHeader
<ArgTypes of={DrawerHeaderStories} />

### DrawerFooter
<ArgTypes of={DrawerFooterStories} />

### DrawerTitle
<ArgTypes of={DrawerTitleStories} />

### DrawerDescription
<ArgTypes of={DrawerDescriptionStories} />
